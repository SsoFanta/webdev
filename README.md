# Simple Catch Game

Open `index.html` in your browser to play.

Controls:
- Arrow keys or `A`/`D` to move left/right
- Move pointer (mouse/touch) over the canvas to control on touch devices
- Click `Restart` to play again

This is a tiny static project — no build steps required.
